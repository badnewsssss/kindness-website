# PayPal Configuration
# Get these from https://developer.paypal.com/dashboard/applications
# 1. Log into developer.paypal.com with your PayPal Business account
# 2. Go to Apps & Credentials
# 3. Create a new app (or use default)
# 4. Copy the Client ID and Secret

# Client ID (safe to expose to browser - used for PayPal buttons)
NEXT_PUBLIC_PAYPAL_CLIENT_ID=your_paypal_client_id_here

# Client Secret (server-only - NEVER expose to browser)
PAYPAL_CLIENT_SECRET=your_paypal_secret_here

# PayPal mode: "sandbox" for testing, "live" for real payments
PAYPAL_MODE=sandbox

# GoFundMe Campaign URL (update with your actual campaign)
NEXT_PUBLIC_GOFUNDME_URL=https://gofundme.com/f/your-campaign

# Admin secret for protected endpoints (GoFundMe offset updates)
# Change this to a strong, unique value in production
ADMIN_SECRET=change_this_to_a_strong_secret
